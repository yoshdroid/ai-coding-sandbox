# Pastel Breakout

`Codex` ディレクトリ配下に作成した、パステルカラーのブロック崩しゲームです。

## 使い方

```bash
python -m breakout
```

`pip install -e .` をしなくても、プロジェクト直下でそのまま起動できます。

## テスト

```bash
pytest
```

## 仕様メモ

- `tkinter` で描画するので追加ライブラリなしで起動できます
- ゲームロジックは UI と分離してあり、`pytest` で検証できます
- ボール、パドル、ブロック、勝敗判定を最小単位でテストしています
