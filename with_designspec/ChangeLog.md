# 変更履歴

このファイルでは、このプロジェクトに対する主な変更を記録します。

## [Unreleased]

### Changed
- `DesignSpec.txt` に合わせてゲームロジックを全面的に再実装
- タイトル画面、ゲーム画面、エンディング画面の遷移を整理
- ステージごとのランダム足場生成を、重ならない決定的な生成方式へ変更
- プレイヤーの移動、ジャンプ、落下、着地、画面外落下時のリスタート処理を調整
- `R` キーによる再実行と `S` キーによるスキップ動作を修正
- pytest のテストを日本語コメント付きで追加・更新
- README を日本語で書き直し、現在の実装内容に合わせて更新
